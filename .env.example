# Next.js public - Cloudflare Worker 聚合接口基础地址
# 线上指向你自己的 Worker 域名（同域下的 /api/* 由 Pages Function 或 Worker 路由接管，无跨域）
NEXT_PUBLIC_API_BASE_URL=

# 本地开发可选：直接指向 Worker 调试地址
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8787
